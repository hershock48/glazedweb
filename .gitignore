node_modules/
.next/
out/
.env*
.DS_Store
*.log
.vercel

# Scratch verification harnesses written at the repo root. Anything worth keeping
# gets cleaned up, documented and moved into tools/ instead.
/*.mjs
